from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The movie dataset is collected and explored. Suitable data handling techinques are applied,including handling missing data and outliers appropriately. Feature engineering involving creating additional features and transforming existing features are applied.The dataset is then split into training and testing set for prediction of %Gross. The various other independent features act as independent variables incluencing the success of a movie. The model is then evaluated.',
    author='2303 2314',
    license='',
)
